spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/saju_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: saju
    password: saju1234

cloud:
  aws:
    endpoint: http://localhost:4566   # LocalStack

cors:
  allowed-origins: http://localhost:3000,http://localhost:8080

app:
  frontend-url: http://localhost:3000

logging:
  level:
    com.company.saju: DEBUG
